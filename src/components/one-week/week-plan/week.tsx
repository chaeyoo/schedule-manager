import {IDay} from "./interface";

interface IWeekProps {
    week: IDay[];
}

export default function Week(props: IWeekProps) {
    const { week } = props;
    return (
        <>

        </>
    )
}